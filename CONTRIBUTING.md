# Contributing

感谢你对这个项目的兴趣！

## 优先贡献方向

- 提示词优化（更准确、更稳定的描述输出）
- Provider 接入与抽象
- 断点续跑 / 重试机制
- 导出格式兼容性
- 审阅与质量抽检工具

## 开发原则

- 核心流程保持简单可理解
- 显式 schema，结构化输出
- 输出便于导出和下游复用
- 避免强 provider 绑定
- 行为变化需在 PR 中说明

## 本地开发

```bash
python -m pip install -e .
make test       # 运行测试
make smoke      # CLI 冒烟测试
make lint       # 语法检查
```

## 提交 PR 前

- 说明解决的问题和关键设计取舍
- 保持 PR 聚焦，避免混合不相关改动
- 涉及输出变化时附上示例

## 需要先开 Issue 讨论的改动

- 改变项目边界（如引入完整 Web 框架）
- 将下游产品逻辑耦合进核心描述流水线
- 大幅修改输出 schema
